# pnpm configuration
# Some React 19 ecosystem packages still declare React 18 peer ranges; do not
# fail the install over them.
strict-peer-dependencies=false
# Keep the isolated node_modules layout (Next.js and Vitest resolve fine with it).
node-linker=isolated
